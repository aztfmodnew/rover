name: Unit tests (ShellSpec)

on:
  push:
    branches: [main]
    paths:
      - 'scripts/**'
      - 'spec/**'
  pull_request:
    paths:
      - 'scripts/**'
      - 'spec/**'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Install Python yq (jq-compatible)
        run: sudo pip3 install --break-system-packages yq

      - name: Install ShellSpec
        run: |
          curl -fsSL https://git.io/shellspec | sh -s -- --yes
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Run unit tests
        env:
          script_path: ${{ github.workspace }}/scripts
          log_folder_path: /tmp/rover-logs
        run: shellspec spec/unit
